const SectionHeadings = ({ title }) => {
  return (
    <section className="quick-search-section-heading mx-4" id="dashboard">
      <h4 className="title-one-head">
        <span className="">{title}</span>
      </h4>
    </section>
  );
};
export default SectionHeadings;
